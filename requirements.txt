# Versions are pinned. Task 11 proves the artefacts rebuild byte-identically,
# but that guarantee only holds against the libraries that built them: an
# unpinned pandas or numpy release moves a committed number, and the
# reproducibility audit reports it as a code change with no way to tell the two
# apart. The pins below are the versions the committed artefacts were built on,
# and they resolve on the 3.11 floor the CI installs (see PYTHON_FLOOR).
#
# To refresh: bump a pin, then re-assert the suite on the floor with
#   uv run --python 3.11 --with-requirements requirements.txt python -m pytest
# and rebuild + commit the tables (python src/run_pipeline.py) in the same pass.

# Core data
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0

# Collection (API-first; respectful HTTP)
requests==2.32.3
httpx==0.27.0
python-dotenv==0.21.0

# NLP
# NOTE: src/preprocess.py (Task 03) deliberately does NOT import spacy or nltk —
# its rules and stopwords are in-repo so all four specialists get identical
# output with no model/corpus download step. These are here for Task 04+.
spacy==3.8.16
scikit-learn==1.5.1
nltk==3.9.1
sentence-transformers==6.0.1

# Testing
pytest==7.4.4

# Forecasting
statsmodels==0.14.2
prophet==1.4.0

# Visualisation
matplotlib==3.10.0
seaborn==0.13.2
plotly==5.24.1

# Notebooks
jupyterlab==4.2.5
ipykernel==6.28.0
