# Task 07 — forecasting validation against scipy and statsmodels

Generated by `src/validate_forecast.py` on 2026-08-16.

`src/forecast.py` imports neither scipy nor statsmodels, so its chi-square tail, normal tail and smoothing recursions are written out by hand. This file is the evidence that the hand-written versions agree with the reference implementations — and, where they do not agree exactly, the measurement of how much the difference could move a published conclusion.

Versions used for the comparison: numpy 1.26.4, scipy 1.13.1, statsmodels 0.14.2.

## Automated checks

- PASS — SES level matches statsmodels at fixed alpha
- PASS — SES SSE matches, so the grid search minimises the same thing
- PASS — damped Holt matches at every horizon in MAX_HORIZON
- PASS — m_loglinear matches OLS on the log share
- PASS — m_ar1 matches OLS through the origin on demeaned lags
- PASS — the sweep drives homogeneity_test's own arithmetic
- PASS — Wilson-Hilferty critical value within 1% of exact for every gate df
- PASS — largest p-value error at the gate's own df range is below 0.002
- PASS — largest p-value error anywhere in the df 2-24 sweep is below 0.01
- PASS — dm_stat equals the one-sample t statistic on the loss differential
- PASS — the erfc tail equals scipy's normal tail
- PASS — no gate verdict flips under the exact chi-square tail
- PASS — no challenger reaches alpha under normal, t, or HLN-corrected t
- PASS — order-statistic coverage matches its claimed level under all three distributions
- PASS — the np.quantile interval undercovers in every cell

## 1. Smoothing recursions against statsmodels

200 random log-share series, 6-14 points, parameters drawn from the module's own grids. Largest absolute disagreement:

| quantity | reference | max abs difference |
| --- | --- | --- |
| SES one-step level | `SimpleExpSmoothing.forecast(1)` | 2.220e-16 |
| SES fitting SSE | `SimpleExpSmoothing.sse` | 3.553e-15 |
| damped Holt, h=1,2,3 | `Holt(damped_trend=True).forecast(h)` | 4.441e-16 |

The damped forecast agrees at all three horizons, which is the part worth stating: the damping sum `phi + phi^2 + ... + phi^h` is the easiest term in the whole module to get wrong by one power, and a one-step-only check would not have seen it.

## 2. Regression models against statsmodels OLS

| model | reference | max abs difference |
| --- | --- | --- |
| `m_loglinear` | `sm.OLS(y, [1, t])` prediction | 1.665e-15 |
| `m_ar1` | `sm.OLS(dev[1:], dev[:-1])`, no intercept | 8.882e-16 |

`m_ar1` is checked against the estimator it actually is, not against `AutoReg`. Demeaning first and regressing through the origin is not the same estimator as fitting an intercept and a slope jointly, and over the same 200 series the two lag-1 coefficients differ by up to 0.142 (median 0.004). Both are defensible on eleven points; claiming they are equal would not be. What matters for Task 07 is that neither wins — `ar1` is the fourth-ranked model on the real backtest and loses to naive.

## 3. Wilson-Hilferty against the exact chi-square tail

Sweep: df 2 to 24, chi-square from 0.2·df to 6·df — wider than the df 7-11 the gate can produce on a twelve-month window.

- largest absolute p-value error anywhere in the sweep: **5.88e-03**, at df 2, chi2 0.95 (approximate 0.627022 against exact 0.621145)
- largest error at the df the gate actually sees (7-11): **1.85e-03**, at df 7, chi2 4.66 (approximate 0.703690 against exact 0.701843)

Two things are worth saying plainly about those numbers. The error is worst at **low df in the body of the distribution**, not in the tail — Wilson-Hilferty is a cube-root normal approximation and it is the small-df shape it struggles with. And it shrinks quickly as df grows, so the range this gate uses is the accurate end of the sweep.

Neither error is where a verdict is decided. What decides a verdict is the critical value at alpha = 0.05:

| df | exact critical chi2 | approximate critical chi2 | gap |
| --- | --- | --- | --- |
| 7 | 14.0671 | 14.0468 | -0.144% |
| 8 | 15.5073 | 15.4891 | -0.118% |
| 9 | 16.9190 | 16.9024 | -0.098% |
| 10 | 18.3070 | 18.2918 | -0.083% |
| 11 | 19.6751 | 19.6610 | -0.072% |

The approximate critical value sits below the exact one, so Wilson-Hilferty rejects a constant share very slightly more readily than the exact tail does. That is the anti-conservative direction for this gate — it errs toward calling a series signal-bearing. A series would have to land inside a window well under one percent wide for the two to disagree; §5 locates the real series relative to it.

## 4. The Diebold-Mariano statistic and its tail

The DM statistic at h=1 with no HAC correction is algebraically the one-sample t statistic on the paired loss differential, which gives a reference implementation that shares none of this module's code.

| quantity | reference | max abs difference |
| --- | --- | --- |
| `dm_stat` | `scipy.stats.ttest_1samp(d, 0).statistic` | 8.882e-16 |
| `p_value` | `2 * scipy.stats.norm.sf(abs(stat))` | 2.220e-16 |

The statistic is exact. The p-value is exactly the two-sided *normal* tail — which is the approximation, and §5 measures what it costs on the real contest.

## 5. The published conclusions, recomputed with scipy

### 5.1 The gate, exact tail against the approximation

| company | chi2 | df | approximate p | exact p | verdict |
| --- | --- | --- | --- | --- | --- |
| databricks | 29.14 | 10 | 1.25e-03 | 1.18e-03 | too_thin |
| google | 38.24 | 10 | 4.22e-05 | 3.44e-05 | forecastable |
| meta | 44.06 | 10 | 4.53e-06 | 3.21e-06 | forecastable |
| microsoft | 67.56 | 10 | 4.62e-10 | 1.31e-10 | too_thin |
| nvidia | 25.68 | 10 | 4.31e-03 | 4.20e-03 | too_thin |
| snowflake | 69.63 | 10 | 2.05e-10 | 5.24e-11 | too_thin |

Every series rejects a constant share under both tails. The weakest, nvidia, sits at an exact p of 4.2e-03 against alpha = 0.05; the strongest is 8 orders of magnitude below that. Nothing is near the boundary where §3's sub-one-percent disagreement could change a verdict — and the four `too_thin` verdicts are refused on cell size, not on this test, so the tail plays no part in them at all.

### 5.2 The refusal under three tails

The normal tail is the most permissive of the three, so it is the one most likely to let a challenger through. Recomputed with Student's t on n-1 df, and again with the Harvey-Leybourne-Newbold small-sample correction (Harvey, Leybourne & Newbold 1997), which shrinks the statistic by `sqrt((n + 1 - 2h + h(h-1)/n) / n)`. Both poolings the task publishes are shown, because the smaller one has twelve paired errors and that is exactly where a normal tail is least defensible:

**all six companies — the pooling behind `model-contest.csv`**

| model | n pairs | dm_stat | p (normal, published) | p (Student t) | p (HLN-corrected t) |
| --- | --- | --- | --- | --- | --- |
| drift | 36 | +1.3494 | 0.1772 | 0.1859 | 0.1919 |
| ses | 36 | -1.2359 | 0.2165 | 0.2247 | 0.2311 |
| mean | 36 | +0.9613 | 0.3364 | 0.3430 | 0.3497 |
| ar1 | 36 | +0.7045 | 0.4811 | 0.4858 | 0.4918 |
| loglinear | 36 | +0.6581 | 0.5104 | 0.5147 | 0.5206 |
| holt_damped | 36 | +0.4446 | 0.6566 | 0.6593 | 0.6638 |

**Google and Meta only — the pair the forecast is published for**

| model | n pairs | dm_stat | p (normal, published) | p (Student t) | p (HLN-corrected t) |
| --- | --- | --- | --- | --- | --- |
| drift | 12 | +1.8443 | 0.0651 | 0.0922 | 0.1051 |
| loglinear | 12 | +1.3124 | 0.1894 | 0.2161 | 0.2350 |
| ses | 12 | -1.0006 | 0.3170 | 0.3385 | 0.3586 |
| holt_damped | 12 | -0.9289 | 0.3529 | 0.3729 | 0.3928 |
| mean | 12 | +0.3035 | 0.7615 | 0.7672 | 0.7768 |
| ar1 | 12 | -0.1505 | 0.8803 | 0.8831 | 0.8880 |

Nothing reaches alpha = 0.05 under either pooling or any of the three tails, and both corrections move every p-value **up**. The published refusal used the easiest of the three tests to pass, and no model passed it.

Read the sign, not only the column: a low p with `dm_stat > 0` is a model that is reliably *worse* than naive, which is why the selection rule in `select_model` requires a negative statistic as well as a small p. `drift` is the case in point — under both poolings it has the smallest p-value of any model while forecasting worse than the benchmark it is being tested against. A rule that read the p-value alone would have published it.

## 6. Prediction-interval coverage by simulation

40,000 replications per cell. Each draws `n` residuals plus one fresh observation, and asks how often the interval built from the first contains the last.

| residual distribution | n | j | claimed coverage | order statistics | `np.quantile` |
| --- | --- | --- | --- | --- | --- |
| normal(0, 1) | 12 | 1 | 0.8462 | 0.8446 | 0.6794 |
| normal(0, 1) | 20 | 2 | 0.8095 | 0.8077 | 0.7248 |
| normal(0, 1) | 40 | 4 | 0.8049 | 0.8036 | 0.7616 |
| t(3), heavy tails | 12 | 1 | 0.8462 | 0.8484 | 0.6848 |
| t(3), heavy tails | 20 | 2 | 0.8095 | 0.8078 | 0.7239 |
| t(3), heavy tails | 40 | 4 | 0.8049 | 0.8052 | 0.7623 |
| lognormal, skewed | 12 | 1 | 0.8462 | 0.8452 | 0.6775 |
| lognormal, skewed | 20 | 2 | 0.8095 | 0.8139 | 0.7330 |
| lognormal, skewed | 40 | 4 | 0.8049 | 0.7997 | 0.7575 |

The order-statistic column tracks the claimed coverage across a normal, a t(3) and a lognormal — the distribution never enters the calculation. Note the claimed level is not 80%: at n = 12 it is 11/13 = 84.6%, because only whole order statistics exist and the module rounds **outward**. `horizon-limits.csv` publishes the achieved level rather than the requested one for that reason.

The last column is the defect this replaced. `np.quantile` interpolates between order statistics, so its endpoints sit strictly inside the sample and its interval is narrower than any interval the sample can actually support — it undercovers in every cell above. On the real backtest that cost was nearly a factor of two: the h=1 interval read 1.84x interpolated against 3.15x measured, which is the difference between clearing the 3x publication limit and failing it.
