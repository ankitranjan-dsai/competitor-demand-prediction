# Task 08 — validating the hand-written similarity statistics

Generated by `src/validate_similarity.py` on 2026-08-18.

`src/similarity.py` carries no scipy import, so five metrics, an average-linkage tree, an average-rank routine, a Fisher interval and two null generators are written by hand. This file is the evidence that the hand-written versions agree with the reference implementations — and, where they do not agree exactly, the measurement of how much the difference could move a published conclusion.

Versions used for the comparison: numpy 1.26.4, scipy 1.13.1, scikit-learn 1.5.1.

## Automated checks

- PASS — cosine matches its reference to 1e-10
- PASS — cosine_sklearn matches its reference to 1e-10
- PASS — jensen_shannon matches its reference to 1e-10
- PASS — bray_curtis matches its reference to 1e-10
- PASS — spearman matches its reference to 1e-10
- PASS — jaccard matches its reference to 1e-10
- PASS — cosine matches scipy on all 15 published pairs to 1e-10
- PASS — merge heights match scipy to 1e-10
- PASS — every cut matches scipy's partition
- PASS — average ranks match scipy to 1e-10
- PASS — tied Spearman matches scipy to 1e-10
- PASS — Fisher bounds match scipy to 0.002
- PASS — identical-null draws match binomial moments to 0.005
- PASS — -1/(D-1) is reproduced by geometric closure to 0.005 at 4, 6 and 10 parts
- PASS — arithmetic closure departs toward zero by under 0.05 at sigma = 0.3
- PASS — calibrate_sigma reproduces the panel's CLR spread to 1%
- PASS — the closure clause holds at the panel's calibrated dispersion

## 1. The five metrics against scipy and scikit-learn

400 random pairs of share vectors over 60 skills, drawn to have the shape real profiles have — mostly small values, about a third exactly zero. Largest absolute disagreement:

| metric | reference | max abs difference |
| --- | --- | --- |
| cosine | `1 - scipy.spatial.distance.cosine` | 0.0000000000 |
| cosine | `sklearn.metrics.pairwise.cosine_similarity` | 0.0000000000 |
| jensen_shannon | `1 - scipy...jensenshannon(base=2)` | 0.0000000000 |
| bray_curtis | `1 - scipy.spatial.distance.braycurtis` | 0.0000000000 |
| spearman | `scipy.stats.spearmanr` | 0.0000000000 |
| jaccard | set overlap, computed directly | 0.0000000000 |

The same check on the six published profiles, on the metric every headline number is quoted on:

| pair | similarity.cosine | scipy | difference |
| --- | --- | --- | --- |
| databricks – google | 0.496119 | 0.496119 | 0.00e+00 |
| databricks – meta | 0.497195 | 0.497195 | 1.67e-16 |
| databricks – microsoft | 0.649827 | 0.649827 | 0.00e+00 |
| databricks – nvidia | 0.575608 | 0.575608 | 0.00e+00 |
| databricks – snowflake | 0.553773 | 0.553773 | 0.00e+00 |
| google – meta | 0.917404 | 0.917404 | 0.00e+00 |
| google – microsoft | 0.670666 | 0.670666 | 0.00e+00 |
| google – nvidia | 0.748531 | 0.748531 | 0.00e+00 |
| google – snowflake | 0.615967 | 0.615967 | 0.00e+00 |
| meta – microsoft | 0.658727 | 0.658727 | 0.00e+00 |
| meta – nvidia | 0.656929 | 0.656929 | 0.00e+00 |
| meta – snowflake | 0.566624 | 0.566624 | 0.00e+00 |
| microsoft – nvidia | 0.676860 | 0.676860 | 0.00e+00 |
| microsoft – snowflake | 0.635125 | 0.635125 | 0.00e+00 |
| nvidia – snowflake | 0.582378 | 0.582378 | 1.11e-16 |

## 2. Average linkage against scipy.cluster.hierarchy

120 random distance matrices of 4 to 8 objects, then the published cosine-distance matrix over the six companies. Every partition from k=2 to k=n-1 is compared as a set of sets, not as labels, because cluster numbering is arbitrary.

| check | result |
| --- | --- |
| max merge-height difference, random matrices | 0.0000000000 |
| partition mismatches, random matrices | 0 |
| max merge-height difference, real profiles | 0.0000000000 |
| partition mismatches, real profiles | 0 |

## 3. Average ranks and tie handling

400 tie-heavy vectors (about 70% of values repeat at this rounding). Largest absolute disagreement:

| quantity | reference | max abs difference |
| --- | --- | --- |
| average ranks | `scipy.stats.rankdata(method='average')` | 0.0000000000 |
| Spearman with ties | `scipy.stats.spearmanr` | 0.0000000000 |

## 4. The Fisher interval against scipy.stats.pearsonr

300 correlated normal samples of 8 to 29 points — the range the panel series actually sits in.

| bound | reference | max abs difference |
| --- | --- | --- |
| lower | `pearsonr(...).confidence_interval(0.95)` | 0.0000161066 |
| upper | `pearsonr(...).confidence_interval(0.95)` | 0.0000136424 |

The residual difference is the normal quantile: `similarity.py` uses the rounded 1.96, scipy uses the exact 1.959964. On an interval already 1.04 wide the difference is in the fourth decimal and cannot move the refusal.

## 5. The identical null and the closure null

**Identical null.** 4000 binomial draws at Google's own sample size (n = 590) over the pooled profile, against `scipy.stats.binom`:

| moment | max abs difference across 127 skills |
| --- | --- |
| mean | 0.000663 |
| variance | 0.00001879 |

**What `-1/(D-1)` is exact for.** Under geometric closure — each period divided by its geometric mean — the analytic value comes back to five decimal places at 2000 periods:

| parts | analytic -1/(D-1) | geometric-closure mean | deviation |
| --- | --- | --- | --- |
| 4 | -0.3333 | -0.333251 | 0.000083 |
| 6 | -0.2000 | -0.199952 | 0.000048 |
| 10 | -0.1111 | -0.111082 | 0.000029 |

The check is a tolerance on the mean rather than containment in a band, because at 2000 periods the band is narrower than the `O(1/n)` bias of a sample correlation — a containment test at this precision measures that bias, not the formula.

**What the data has instead.** Shares are closed by their sum. The same simulation under arithmetic closure lands short of the analytic value, always toward zero, with a further small attenuation at the panel's eleven periods:

| parts | analytic | arithmetic closure, n = 2000 | gap | arithmetic closure, n = 11 |
| --- | --- | --- | --- | --- |
| 4 | -0.3333 | -0.3191 | +0.0143 | -0.3080 |
| 6 | -0.2000 | -0.1911 | +0.0089 | -0.1824 |
| 10 | -0.1111 | -0.1061 | +0.0050 | -0.1009 |

The gap is not a defect in either implementation. It is the reason `trajectory_verdict` reads the simulated band and never the formula, and the reason figure 08 draws `-1/(D-1)` as a dashed line outside the band rather than through the middle of it.

**Calibration.** The gap grows with dispersion, so the published band is run at the panel's own. The six companies' CLR coordinates have spread 0.8047; `calibrate_sigma` returns sigma = 0.8901, which reproduces a spread of 0.8046 — 0.00% off the target.

**What rests on it.** `closure-sensitivity.csv` runs the clause across the dispersion range. It holds at the calibrated sigma (yes) and first fails at sigma = 1.20, where the observed mean falls *below* the band — i.e. more negative than independent series produce, which is still not evidence that these companies move together. The refusal also rests on two reasons that do not involve the null at all: one eligible pair out of fifteen, and a mean interval spanning half the range.
